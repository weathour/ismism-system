interface:
  display_name: "ISMISM Knowledge Operator"
  short_description: "Traceable ISMISM plugin operation"
  default_prompt: "Use $ismism-knowledge-operator to answer from ISMISM Library with row, segment, path, quote, and validator traceability."
policy:
  allow_implicit_invocation: true
